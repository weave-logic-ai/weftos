//! Template for `wifi_secrets.rs`. Copy this file to `wifi_secrets.rs`
//! (same directory, drop the `.example`) and fill in real values.
//! `wifi_secrets.rs` is gitignored — credentials never go in git.
pub const WIFI_SSID: &str = "your-ssid-here";
pub const WIFI_PASSWORD: &str = "your-password-here";
